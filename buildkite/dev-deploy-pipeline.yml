env:
  HINTR_BRANCH: master
  HINT_BRANCH: master
  deploy: all


steps:
  - label: ":rocket: dev"
    command: "ssh -i ~/.ssh/id_hiv -oStrictHostKeyChecking=accept-new vagrant@naomi-dev.dide.ic.ac.uk 'cd hint-deploy && git pull && ./hint upgrade --hintr-branch=$HINTR_BRANCH --hint-branch=$HINT_BRANCH $DEPLOY'"
    agents:
      queue: "hint-deploy"

